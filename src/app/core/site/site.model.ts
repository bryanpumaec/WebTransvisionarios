export interface Servicio {
  slug: string;
  titulo: string;
  resumen: string;
}

export interface SiteConfig {
  nombre: string;
  tagline: string;
  telefono: string;
  email: string;
  direccion: string;
}
